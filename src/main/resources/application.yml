server:
  port: '8088'

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/empdata
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'

welcome:
  message: '"Hello !! This is Spring Boot Project."'

management:
  endpoints:
    web:
      exposure:
        include: "*"